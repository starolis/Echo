[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "npm run dev -- --port 3456"
  framework = "#custom"
  targetPort = 3456
  port = 3000
  autoLaunch = false
